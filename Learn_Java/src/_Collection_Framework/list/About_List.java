package _Collection_Framework.list;

public class About_List {
	/*
	 * LIST 
	 * 
	 * The List interface is found in the java.util package and inherits the
	 * Collection interface. It is a factory of ListIterator interface. Through the
	 * ListIterator, we can iterate the list in forward and backward directions. The
	 * implementation classes of List interface are ArrayList, LinkedList, Stack and
	 * Vector.
	 * 
	 * List Interface declaration
	 *    public interface List<E> extends Collection<E> 
	 */
}
